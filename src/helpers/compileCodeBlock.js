export const compileCodeBlock = (paInfo) => {
	const id = paInfo.id.value.en;
	// const md = `~~~js
	//         if(app_client_id && app_client_id == {{id}}){
	//             console.log("Hello World");
	//         }

	//     `;
	const md = "";
	return md;
};
